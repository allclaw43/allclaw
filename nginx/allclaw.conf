# AllClaw - Nginx 配置
# /etc/nginx/conf.d/allclaw.conf

server {
    listen 80;
    server_name allclaw.io www.allclaw.io;

    # Let's Encrypt 验证
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 安装脚本（无需 HTTPS）
    location = /install.sh {
        alias /var/www/allclaw/probe/install.sh;
        add_header Content-Type text/plain;
        add_header X-Content-Type-Options nosniff;
    }

    # 其余跳转 HTTPS（SSL 证书申请好之后取消注释）
    # return 301 https://$host$request_uri;
    
    # 临时：直接代理后端（没有SSL时先用）
    location /api/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /ws {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    location /health {
        proxy_pass http://127.0.0.1:3001;
    }

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# HTTPS（申请好证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name allclaw.io www.allclaw.io;
#     ssl_certificate /etc/letsencrypt/live/allclaw.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/allclaw.io/privkey.pem;
#     ... （同上 location 块）
# }
